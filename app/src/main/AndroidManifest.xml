<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLoginApp"
        tools:targetApi="31">

        <!-- Establece RegisterActivity como pantalla de inicio -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="true"/>
        <activity android:name=".MenuActivity" android:exported="true"/>
        <activity android:name=".AlertSettingsActivity" android:exported="true"/>
        <activity android:name=".CreateDataActivity" android:exported="true"/>
        <activity android:name=".ReadDataActivity" android:exported="true"/>
        <activity android:name=".DeleteDataActivity" android:exported="true"/>
        <activity android:name=".UpdateDataActivity" android:exported="true"/>

    </application>

</manifest>

